OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Hugging Face Router (OpenAI-compatible)
# Crie um token em https://huggingface.co/settings/tokens e use como HF_TOKEN
HF_TOKEN=
HF_BASE_URL=https://router.huggingface.co/v1
# Exemplo de modelo (ajuste para o modelo que você quer usar no Router)
HF_MODEL=deepseek-ai/DeepSeek-R1-Distill-Qwen-32B

# Opcional (fallback)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash